<p>메트릭스 차트 화면 예시를 통해 선택한 시간에 모니터링 대상으로부터 수집한 지표(<code>{props.metric}</code>)를 차트로 확인할 수 있습니다. 이때 수집 서버는 선택한 지표인 <code>{props.metric}</code> 값을 필드로 가지는 <code>{props.category}</code> 카테고리 메트릭스를 사용합니다.</p>