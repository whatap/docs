리눅스 컨테이너의 CPU, MEMORY, DISK, NIC 성능을 수집합니다. 수집된 성능 정보는 <Cmdname sid="category" className="uitext" /> `server_container`에서 확인할 수 있습니다.

```bash title='Linux Shell'
echo "container.enabled=true" |sudo tee -a /usr/whatap/infra/conf/whatap.conf
```