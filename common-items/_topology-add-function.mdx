토폴로지 메뉴에서 이용할 수 있는 부가 기능에 대한 설명을 제공합니다.

## 하이라이트 필터

특정 노드를 선택해 연관 관계를 가진 노드를 강조하여 보여주기 위한 기능을 제공합니다. 대량 노드 간의 복잡한 연관 관계를 토폴로지로 용이하게 확인할 수 있습니다.

![하이라이트 필터](/img/topology-highlight.png)

* 특정 노드에 마우스 오버하여 해당 노드 및 연관 노드, 링크 정보를 하이라이트로 표시합니다.
* 키워드 입력을 통해 원하는 노드의 선택을 유지할 수 있습니다. 토폴로지 갱신 시에도 하이라이트 상태는 유지합니다.

## 노드선택 기능 이용하기

화면 오른쪽 아래에 ***노드선택(더블 클릭)*** 상자를 더블 클릭하세요. ***노드선택*** 상자에서 오른쪽의 하위 노드를 클릭하면 하이라이트 필터를 적용할 수 있습니다.

![노드 선택](/img/topology-select-node.png)

본 차트에는 프로젝트, DB 호출, 외부 호출 노드를 그룹핑하여 다이어그램으로 제공하고, ***노드선택*** 상자에서 오른쪽의 하위 노드를 클릭하면 해당 노드의 명칭이 하이라이트 필터에 적용됩니다.

***노드 선택*** 상자를 더블 클릭하면 다음의 기능을 이용할 수 있습니다.

* 줌인/줌아웃
* 드래그
* 하위 노드 클릭 시 하이라이트 필터 적용
* 상위 노드 클릭 시 하위 노드 숨김 또는 표시

***노드선택*** 상자로 하이라이트 필터 기능을 이용하지 않을 경우 외곽 점선을 더블 클릭하거나 ***노드선택 감추기(더블클릭)***를 더블 클릭하세요.

:::note

메뉴에 따라 ***노드선택*** 기능의 위치는 다를 수 있습니다. ***애플리케이션 토폴로지*** 메뉴에서는 ***노드선택*** 버튼으로 메뉴를 제공합니다.

:::

## 가변 폰트

차트에 표현하는 정보가 많아질수록 줌아웃으로 차트를 축소해서 보게 될 가능성이 높습니다. 차트를 축소하게 되면 노드의 위치는 고정하여 노드 식별은 가능할 수 있으나, 글자의 사이즈가 상대적으로 작아져 호출 정보를 식별하기 어려울 수 있습니다. 토폴로지 차트에서는 폰트 크기를 노드 간 링크의 거리에 비례하여 표시합니다.

![가변 폰트](https://img.whatap.io/media/user_guide_application/topology/dynamic_font_size.png)

## 컨텍스트 메뉴

토폴로지의 노드에서 마우스 오른쪽 버튼을 클릭하면 컨텍스트 메뉴가 나타납니다. 컨텍스트 메뉴에는 애플리케이션 모니터링 대시보드로 이동하기 위한 링크 등의 메뉴를 제공합니다.

![컨텍스트 메뉴](https://img.whatap.io/media/user_guide_application/topology/context_menu.png)

:::note

***컨테이너 맵***, ***노드 자원***, ***컨테이너 자원*** 메뉴는 컨테이너 모니터링에서만 제공합니다.

:::

## 노드 위치 저장 및 복원

토폴로지 화면에서 차트의 노드 위치는 마우스 왼쪽 버튼을 클릭한 상태에서 드래그해 변경할 수 있습니다. 변경한 노드의 위치는 다른 메뉴로 진입한 후 토폴로지 화면으로 다시 돌아와도 유지됩니다.

* ***위치 저장***: 사용자가 변경한 노드의 위치 정보를 저장할 수 있습니다.
* ***위치 복원***: 사용자가 위치 저장을 실행한 시점으로 노드의 위치를 복원합니다.
* ***위치 초기화***: 노드의 위치를 초기화합니다.

:::note

* 노드 위치 메뉴는 사용자가 진입한 토폴로지 메뉴에 따라 지원하지 않을 수 있습니다.
* 변경한 노드의 위치 정보는 웹 브라우저의 로컬 스토리지에 저장됩니다. 다른 사용자의 화면에서는 변경된 노드 위치가 반영되지 않습니다.

:::

## 별칭 부여 및 제거

토폴로지 화면 오른쪽 하단의 ***노드선택*** 상자를 통해서 노드의 별칭을 설정할 수 있습니다.

1. ***노드선택(더블 클릭)*** 상자를 더블 클릭하세요.

1. 별칭을 설정할 노드로 이동해 마우스 오른쪽 버튼을 클릭하세요.

    ![별칭 입력](/img/node-custom-name.png)

1. 별칭 입력 창이 나타나면 별칭을 입력 후 엔터를 입력하세요.

별칭 정보를 저장합니다. 차트의 데이터를 새로 조회하는 시점에서 저장한 별칭을 적용합니다.

저장한 별칭을 제거하려면 토폴로지 화면 메뉴에서 ***별칭 초기화*** 버튼을 선택하세요. 저장한 별칭 정보를 삭제합니다. 차트 데이터를 새로 조회하는 시점에서 화면에 적용합니다.

:::note

노드 별칭 정보는 웹 브라우저의 로컬 스토리지에 저장됩니다.

:::

## 인바운드/아웃바운드 필터

***인바운드***/***아웃바운드*** 토글 버튼을 이용해 토폴로지 화면에 외부 모듈 및 외부 호출 노드의 표시 여부를 변경할 수 있습니다.

* 인인바운드/아웃바운드 표시 상태

  ![인바운드/아웃바운드 표시 상태](https://img.whatap.io/media/user_guide_application/topology/inbound_outbound_displayed.png)

* 인인바운드/아웃바운드 숨김 상태

  ![인바운드/아웃바운드 비표시 상태](https://img.whatap.io/media/user_guide_application/topology/inbound_outbound_hidden.png)

## 임계치 초과 정보 강조

노드 사이의 호출 응답 시간 및 에러 건수 임계치 설정 시 임계치를 초과한 노드 및 호출 관계를 강조해 표시합니다. 토폴로지 화면의 메뉴에서 ***응답 시간(ms)***과 ***에러 건수*** 텍스트 입력 상자에 원하는 설정값을 입력하세요.

![임계치 초과 정보 강조](https://img.whatap.io/media/user_guide_application/topology/threshold_emphasized.png)

:::note

***애플리케이션 토폴로지*** 메뉴에서는 ***경고 표시***를 선택한 다음 설정값을 입력할 수 있습니다.

:::