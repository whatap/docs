---
id: agent-number-of-user
title: 사용자 수
description: 자바(Java) 웹 애플리케이션 서버에 연결된 사용자 수를 에이전트 설정을 통해 집계하는 방법을 안내합니다. 실시간 사용자 집계 활성화 여부 설정, 사용자 수를 집계하기 위한 쿠키 제한 설정, IP 주소 또는 HTTP 헤더 특정 값을 기준으로 한 사용자 집계 방법, 클라이언트 IP 정보 추적을 위한 HTTP 헤더 설정 등을 포함합니다.
tags: [ Java, 애플리케이션, 에이전트 ]
---

자바(Java) 웹 애플리케이션 서버에 연결된 사용자 수를 에이전트 설정을 통해 집계하는 방법을 안내합니다. 실시간 사용자 집계 활성화 여부 설정, 사용자 수를 집계하기 위한 쿠키 제한 설정, IP 주소 또는 HTTP 헤더 특정 값을 기준으로 한 사용자 집계 방법, 클라이언트 IP 정보 추적을 위한 HTTP 헤더 설정 등을 포함합니다.

* **trace_user_enabled** <span class='type'>Boolean</span>

  기본값 `true`

  실시간 사용자 집계 여부를 설정합니다. 사용자 추적 옵션이 중복 설정된 경우 동작 우선 순위는 다음과 같습니다.
  
  >   1. **trace_user_using_ip** / **wclient_using_ip**
  >   1. **trace_user_using_jsession**
  >   1. **user_header_ticket** / **wclient_header_ticket**

  :::note

  Java 에이전트 2.2.0 버전 이상의 경우 `wclient_using_ip`, `wclient_header_ticket` 옵션을 이용하세요.

  :::

import Commoncollectuser from "../common-items/_common-collect-user.mdx";

<Commoncollectuser />