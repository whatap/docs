---
id: node-details
title: 노드 상세
description: 노드 상세 정보를 확인할 수 있습니다.
tags: [ 쿠버네티스, 쿠버네티스 모니터링, 클러스터, 노드 상세 ]
---

홈 화면 > 프로젝트 선택 > <Cmdname sid="cluster" className="uitext" /> > <Cmdname sid="node_detail" className="uitext" />

***노드 상세*** 화면은 상단의 4개 차트와 노드 내 ***컨테이너*** 및 ***Pod*** 상세 정보로 구성되어 있습니다. 노드 셀렉터를 통해 조회를 원하는 노드를 선택할 수 있습니다.

<ImgLang img='k8s-resource-node-detail.png' desc='노드 상세 화면' />

:::note 

5초 주기로 최근 1분 데이터를 업데이트합니다.

:::

### 상단 차트

다음과 같은 4개의 차트 정보를 확인할 수 있습니다.

* ***CPU Usage***: 노드의 CPU 사용량을 확인할 수 있습니다.

* ***Memory Usage***: 노드의 메모리 사용량을 확인할 수 있습니다.

* ***Network***: 노드의 네트워크 인터페이스의 입출력 트래픽과 오류 패킷을 확인할 수 있습니다.

* ***Disk Usage***: 노드의 Disk 사용량을 확인할 수 있습니다. 

### 컨테이너 및 Pod 상세 정보 

#### 컨테이너

***컨테이너*** 탭 선택 시 노드 내 컨테이너 상세 정보 상단에서 `cpu_quota`, `cpu_request`, `cpu_total_milli`, `mem_limit`, `mem_request`, `mem_working_set` 지표의 합계를 확인할 수 있습니다. 

* 원하는 컬럼 선택 시 해당 컬럼 기준으로 컨테이너 목록을 정렬해 조회할 수 있습니다. 

* 컨테이너 선택 시 ***컨테이너 목록*** 메뉴로 이동합니다. 자세한 내용은 [다음 문서](resource-container-list)를 참조하세요.

{/* 컨테이너 상세 정보는 다음 내용을 제공합니다. 

| 항목           | 설명                                              |
| ---------------- | ------------------------------------------------- |
| 상태           | 컨테이너 상태                                     |
| 컨테이너       | Container ID                                      |
| 이미지         | 컨테이너 이미지 정보                              |
| 커맨드         | 컨테이너 실행 명령                                |
| 애플리케이션   | 컨테이너에 설치된 와탭 APM 에이전트의 이름        |
| Pod            | Pod 이름                                          |
| CpuByLimit     | CPU Limit 기준 CPU 전체 사용률                    |
| MemoryWsByLimit| 컨테이너 메모리 Limit 기준 working set 사용량     |
| CpuThrottledCnt| 컨테이너 CPU Throttled 건수                       |
| CpuThrottledTime | 컨테이너 CPU Throttled 시간                       |
| MemLimit       | 컨테이너 메모리 Limit 크기                        |
| MemRequest     | 컨테이너 메모리 Request 크기                      |
| MemWs          | 컨테이너 메모리 working set                       |
| IoReadBytes    | 컨테이너 전체 블럭 디바이스의 초당 읽은 바이트 합 |
| IoReadIops     | 컨테이너 전체 블럭 디바이스의 초당 읽은 건수 합   |
| IoWriteBytes   | 컨테이너 전체 블럭 디바이스의 초당 쓴 바이트 합   |
| IoWriteIops    | 컨테이너 전체 블럭 디바이스의 초당 쓴 건수 합     |
| NetRxBytes     | 컨테이너 전체 블럭 디바이스의 초당 읽기 바이트 합 |
| NetRxDropped   | 컨테이너 네트워크 수신 dropped 건수               |
| NetRxError     | 컨테이너 네트워크 수신 에러 건수                  |
| NetRxIops      | 컨테이너 네트워크 수신 건수                       |
| NetTxBytes      | 컨테이너 네트워크 송신 데이터 크기                |
| NetTxDropped   | 컨테이너 네트워크 송신 dropped 건수               |
| NetTxError     | 컨테이너 네트워크 송신 에러 건수                  |
| NetTxIops      | 컨테이너 네트워크 송신 건수                       | 
*/}

#### Pod

***Pod*** 탭 선택 시 Pod 상세 정보를 조회합니다. 상세 정보 상단에서 `cpu_quota`, `cpu_request`, `cpu_total_milli`, `mem_limit`, `mem_request`, `mem_working_set` 지표의 합계를 확인할 수 있습니다. 

* 원하는 컬럼 선택 시 해당 컬럼 기준으로 Pod 목록을 정렬해 조회할 수 있습니다. 

* Pod 선택 시 ***Pod 목록*** 메뉴로 이동합니다. 자세한 내용은 [다음 문서](resource-pod-list)를 참조하세요. 

{/* Pod 상세 정보는 다음 내용을 제공합니다. 

| 항목          | 설명                             |
| ------------- | -------------------------------- |
| Pod           | Pod의 고유한 이름                |
| PodIp         | Pod의 고유한 IP 주소 |
| Namespace     | Pod가 속한 네임스페이스          |
| Node          | Pod가 실행 중인 노드 이름        |
| ConRestartCnt | 컨테이너 재시작 횟수             |
| Age           | Pod가 실행된 시간                |
| CpuTotUsage   | Pod의 총 CPU 사용량              |
| CpuRequest    | Pod가 요청한 CPU 리소스          | 
*/}