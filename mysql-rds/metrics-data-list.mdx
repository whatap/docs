---
id: metrics-data-list
title: MySQL RDS 지표
description: MySQL RDS에서 사용하는 중요 지표를 안내합니다.
tags: [ MySQL RDS, 메트릭스, 데이터베이스, 데이터베이스 모니터링 ]
---

데이터베이스 메트릭은 데이터베이스의 작업량과 리소스 사용량을 포괄적으로 수집합니다. 와탭 모니터링 서비스는 지속적으로 메트릭을 수집하여 데이터베이스의 병목 현상을 확인하고, 성능 문제의 근본 원인을 식별하는 데 도움이 되는 데이터를 제공합니다.

## 주요 지표

| 지표 이름 | 설명 |
| -------------------------------- | ----------------------------------------------------------------------------------------- |
| Innodb Buffer Pool Read Requests | 논리 read 요청 횟수, Logical read + Physical read. Logical read의 값이 크므로 Logical read의 개념으로 사용합니다. |
| Innodb Buffer Pool Reads | 물리적 read를 한 횟수, Physical read |
| Threads Connected | 현재 접속되어 있는 세션 수 |
| Threads Running | 쿼리를 실행 중인 세션 수 |
| Questions | 실행된 query의 수 |
| Com Select | Select 명령을 실행한 횟수 |
| Writes | write에 사용한 명령 횟수, `Com_insert` + `update` + `delete` |
| Bytes Received | db 서버가 받은 데이터 bytes |
| Bytes Sent | db 서버가 보낸 데이터 bytes |
| Innodb Row Lock Current Waits | Lock의 수 |
| Connections | DB에 접속 시도 횟수 |
| Com Commit | commit 횟수 |
| Com Rollback | rollback 횟수 |
| Innodb Rows Read | select에서 처리된 row 수 |
| Innodb Rows Writes | write에서 처리된 row 수, `Innodb_rows_inserted` + `updated` + `deleted` |
| Innodb Row Lock Waits | row lock wait 한 수 |
| Innodb Row Lock Time | row lock wait 한 시간 |
| Sort Rows | sort 처리한 row 수 |


## MySQL 메트릭 

<div class='db-metric'>

| 지표 이름 | 설명 |
| -- | -- |
| `Aborted_connects` | MySQL서버에 연결하기 위해 시도한 실패 수 |
| `active_sessions` | 쿼리를 실행중인 세션 수. `Threads_running`과 같음|
| `Connection_errors_internal` | 내부 서버 오류, 예를 들어 메모리 오류가 발생하지 않거나 실패 스레드가 시작됩니다.글로벌 값은 플러시 상태로 플러시 될 수 있습니다. |
| `Connection_errors_max_connections` | `max_Connections` 한계에 도달하여 연결된 연결 수.글로벌 값은 플러시 상태로 플러시 될 수 있습니다.|
| `Connections` | DB 에 접속 시도 횟수 |
| `Threads_connected` | 현재 접속 되어 있는 세션 갯수 |
| `max_connections` | 최대 동시 클라이언트 연결 수. 커넥션 수가 부족할 경우 'Too ManyConnection Error'가 발생할 수 있습니다. |
| `Bytes_received` | db서버가 받은 데이터 bytes. `Bytes_received` 가 높다면 이 서버는 쓰기 위주작업(insert 등) 서버임을 알 수 있습니다. |
| `Bytes_sent` | db 서버가 보낸 데이터 bytes. `Bytes_sent`가 높다면 이 서버는 읽기위주 작업(select) 서버임을 알 수 있습니다. `Questions`, `Uptime`과 함께 응용해보면 1개 쿼리당 평균 Byte, 시간당 처리 Byte등을 계산 할 수 있습니다.|
| `Innodb_buffer_pool_bytes_dirty` | 버퍼풀의 데이터 중 변경된 바이트 수(더티 데이터 수)|
| `Innodb_buffer_pool_pages_data` | 전체 버퍼풀에서 현재 사용중인 버퍼풀 페이지 수|
| `Innodb_buffer_pool_pages_dirty` | 버퍼풀의 데이터 중 변경된 페이지 수(더티 페이지 수)|
| `Innodb_buffer_pool_pages_flushed` | 플러시 된 InnoDB 버퍼풀 페이지 수. 이는 버퍼풀의 더티 데이터를 디스크에 쓰는 과정으로 과도한 flush는 디스크에 부담을 줄 수 있으므로 디스크의 성능과 함께 모니터링 해야 합니다.|
| `Innodb_buffer_pool_pages_free` | 무료 innodb 버퍼 풀 페이지 수. |
| `Innodb_buffer_pool_pages_total` | 전체 버퍼풀의 페이지 수 |
| `Innodb_buffer_pool_read_requests` | 논리적 읽기 요청 수(물리적 읽기를 포함). Inodb buffer pool hit율 계산을 다음과 같이 할 수 있으며, 90% 이상을 권장합니다. 만약 Hit Ratio가 낮다면 Innodb_buffer_pool_size증설을 검토합니다.<br/>`Buffer Hit Ratio = (1 - (Innodb_buffer_pool_reads/Innodb_buffer_pool_read_requests)) * 100` |
| `Innodb_buffer_pool_reads` | 물리적(Disk) read 를 한 횟수. Innodb 버퍼풀에 없어서 디스크에서 직접읽어야 하는 논리적 읽기 요청 수 |
| `Binlog_bytes_written` | 이진 로그에 기록 된 바이트 수.|
| `Binlog_commits` | 이진 로그에 약속 된 총 거래 수. |
| `Innodb_row_lock_time` | row lock 획득에 소비된 총 시간 (milliseconds)|
| `Innodb_row_lock_waits` | Innodb 스토리지 엔진 테이블에서 row lock을 기다려야 하는 횟수. Lock을 획득한 수가 아닌 lock을 얻기 위해 대기하는 횟수로 이 지표가 평소보다 급증한다면 쿼리 대기가 발생(락 경합)하는 것으로 쿼리 튜닝이 필요합니다.|
| `lock_wait_sessions` | 현재 row lock을 획득하기 위해 대기중인 수. `Innodb_row_lock_current_waits`과 같음|
| `replication_delay` | Replication구조일 경우 replication db에 반영되는 지연시간. Slave DB에서 측정되는 지표입니다. |
| `Slave_heartbeat_period` | 하트 비트 패킷이 노예가 마스터로부터 요청하는 초의 시간.|
| `Slaves_running` | 실행되는 슬레이브 SQL 스레드 수.|
| `Slow_queries` | `long_query_time` 초 이상 걸린 쿼리 수 |
| `Sort_rows` | sort 처리한 row 수|
| `Com_commit` | commit 횟수 |
| `Com_delete` | 실행된 삭제 명령 수 `handler_delete`와는 다른 행이 테이블에서 삭제 된 횟수를 계산합니다.|
| `Com_insert` | 실행된 삽입 명령 수|
| `Com_rollback` | rollback 횟수 |
| `Com_select` | Select 명령을 실행한 횟수 |
| `Com_update` | 실행된 업데이트 명령 수. |
| `Innodb_rows_read` | select 에서 처리된 row 수 |
| `Innodb_rows_writes` | write 에 처리된 row 수. `Innodb_rows_inserted` + `Innodb_rows_updated` + `Innodb_rows_deleted` 와 같음|
| `Queries` | `com_ping` 및 `com_statistics를` 제외한 서버에서 실행 한 명령문 수. Stored program 내에서 실행된 진술도 계산한다는 점에서 `Questions`과 다릅니다. |
| `Questions` | 실행된 query 의 갯수. `COM_PING`, `COM_STATISTICS`, `COM_STMT_PREPARE`, `COM_STMT_CLOSE` 및 `COM_STMT_RESET` 문을 제외한 서버에서 실행한 문 수입니다. Stored program 내에서 실행된 명령문을 계산하지 않는다는 점 에서 `Queries` 와 다릅니다 .|
| `Writes` | write 에 사용한 명령 횟수. `Com_insert` + `Com_update` + `Com_delete` 와 같음|
| `Aborted_clients` | 정상적으로 종료되지 않은 클라이언트로 인해 중된된 연결 수. |
| `Access_denied_errors` | 접근 거부 오류 수. |
| `Connection_errors_accept` | 청취 포트에서 호출을 요청하는 동안 발생한 오류 수.글로벌 값은 플러시 상태로 플러시 될 수 있습니다. |
| `Connection_errors_peer_address` | 연결 클라이언트 IP 주소를 검색하는 동안 오류 수글로벌 값은 플러시 상태로 플러시 될 수 있습니다.|
| `Connection_errors_select` | 청취 포트에서 `select ()` 또는 `poll ()`을 호출하는 동안 오류 수. 이 경우 고객이 반드시 거부되지는 않았을 것입니다. 글로벌 값은 플러시 상태로 플러시 될 수 있습니다. |
| `Connection_errors_tcpwrap` | 연결 횟수 LibwRap 라이브러리가 거부했습니다. 글로벌 값은 플러시 상태로 플러시 될 수 있습니다.|
| `Threads_running` | sleep 상태가 아니고 active하게 command를 실행중인 Client Connection 수. 일부 내부 시스템 스레드는 Show ProcessList 문의 출력에 표시되는 경우이 상태 변수에도 계산됩니다.|
| `Delayed_errors` | 삽입 중에 발생한 오류 수는 지연되었습니다. 글로벌 값은 플러시 상태로 플러시 될 수 있습니다.|
| `Delayed_insert_threads` | 지연된 스레드 삽입 수. |
| `Delayed_writes` | 삽입 지연 행 수. 글로벌 값은 플러시 상태로 플러시 될 수 있습니다.|
| `Delete_scan` | 전체 테이블 스캔이 필요한 삭제 수.|
| `Empty_queries` | 결과가없는 쿼리 수. 이것은 `com_empty_query`와 같지 않습니다.|
| `Executed_events` | Create Event와 함께 생성 된 이벤트가 실행되었습니다. 이는 이벤트가 실행될 때만 증가 할 때만 증가한다는 점에서 `com_create_event`와 다릅니다. |
| `Executed_triggers` | Create Trigger와 함께 생성 된 트리거가 횟수로 실행되었습니다. 이는 트리거가 실행될 때만 발생할 때만 증가한다는 점에서 `com_create_trigger`와 다릅니다.|
| `Feature_check_constraint` | 제약 조건 수가 확인되었습니다.글로벌 값은 플러시 상태로 플러시 될 수 있습니다. |
| `Feature_custom_aggregate_functions` | 사용자 정의 집계 기능을 사용하는 쿼리 수.|
| `Feature_delay_key_write` | `Delay_key_write`를 사용하는 테이블 수입니다.글로벌 값은 플러시 상태로 플러시 될 수 있습니다. |
| `Feature_dynamic_columns` | `column_create ()` 함수가 사용 된 횟수입니다.|
| `Feature_fulltext` | 일치하는 횟수… () 함수에 대해 사용되었습니다. |
| `Feature_gis` | 기하학적 열이있는 테이블이 열려있었습니다. |
| `Feature_invisible_columns` | 열린 모든 테이블에서 보이지 않는 열의 수.|
| `Feature_json` | JSON 기능 중 하나와 같은 JSON 기능이 사용 된 횟수입니다. Connect Engine JSON 유형은 포함되지 않거나 Format = JSON을 설명/분석합니다.|
| `Feature_locale` | @@ `lc_messages` 변수가 할당되었습니다. |
| `Feature_subquery` | 사용 된 하위 쿼리 수 (From Clause의 하위 쿼리 제외). |
| `Feature_system_versioning` | 시스템 버전화 기능이 사용되었습니다 (시스템 버전화가있는 테이블 열기).|
| `Feature_timezone` | 명시적인 시간대 (UTC 및 시스템 제외)가 지정되었습니다. |
| `Feature_trigger` | 트리거 수를로드했습니다. |
| `Feature_window_functions` | 윈도우 기능이 사용되었습니다.|
| `Feature_xml` | XML 함수 (`ExtractValue ()` 및 `UpdateXml ()`)가 사용되었습니다. |
| `Flush_commands` | 내부 서버 플러시 요청뿐만 아니라 실행된 플러시 문의 수. 이는 내부 서버 플러시 작업이 아닌 플러시 문을 간주하는 `com_flush`와 다릅니다. |
| `Uptime` | 서버가 실행중인 초|
| `Uptime_since_flush_status` | 마지막 플러시 상태 이후 몇 초.|
| `Cpu_time` | 사용 된 총 CPU 시간.|
| `Created_tmp_disk_tables` | 명령문을 실행하는 동안 서버에서 디스크에 생성한 임시 테이블 개수. 디스크 사용은 성능을 저하시킬 수 있으므로 이 값이 커지면 `tmp_table_size` 또는 `max_heap_table_size`값 조정을 검토합니다.|
| `Created_tmp_files` | mysqld가 생성한 임시 파일 수 |
| `Created_tmp_tables` | 명령문을 실행하는 동안 서버에 의해 생성된 내부 임시 테이블의 수. 일반적으로 쿼리에서 UNION ALL을 사용하거나 ORDER BY,GROUP BY,DISTINCT를 사용하는 몇가지 경우 임시테이블이 사용될 수 있습니다. 총 내부 임시테이블 수(`Created_tmp_disk_tables` + `Created_tmp_tables`) 대비 디스크상의 임시테이블 수(`Created_tmp_disk_tables`)가 높지 않은 것이 좋습니다.(10%미만) |
| `Innodb_available_undo_logs` | 사용 가능한 총 수 Innodb Undo Logs. `innodb_undo_logs` 시스템 변수와 다르며 활성 취소 로그 수를 지정합니다. |
| `Innodb_background_log_sync` | Show Engine InnoDB 상태 출력의 배경 스레드 섹션에 표시된대로. |
| `Innodb_buffer_pool_bytes_data` | 전체 버퍼풀에서 현새 사용중인 버퍼풀 바이트 수|
| `Innodb_buffer_pool_pages_LRU_flushed` | 플러시 목록 삽입 버퍼 및 적응 형 해시 인덱스 Show Engine InnoDB 상태 출력 섹션. |
| `Innodb_buffer_pool_pages_made_not_young` | Show Engine InnoDB 상태 출력의 삽입 버퍼 및 적응 형 해시 인덱스 섹션에 표시된 것처럼 젊지 않은 페이지.|
| `Innodb_buffer_pool_pages_made_young` | 페이지는 Show Engine InnoDB 상태 출력의 삽입 버퍼 및 적응 형 해시 인덱스 섹션에 표시된 것처럼 젊게 만들었습니다. |
| `Innodb_buffer_pool_pages_misc` | Row lock, hash index와 같이 오버헤드에 할당되어 사용된 버퍼풀의 페이지 수|
| `Innodb_buffer_pool_pages_old` | 전형 데이터베이스 페이지, 버퍼 풀 및 메모리 섹션의 Show Engine InnoDB 상태 출력에 표시된대로.|
| `Innodb_buffer_pool_read_ahead` | 미리읽기 백그라운드 스레드가 버퍼풀로 미리 읽어들인 페이지 수|
| `Innodb_buffer_pool_read_ahead_evicted` | 버퍼풀에 미리 읽어들인 페이지 중 사용되지 않고 제거된 페이지 수|
| `Innodb_buffer_pool_read_ahead_rnd` | 랜덤으로 미리 읽기가 발생한 페이지 수 |
| `Innodb_buffer_pool_wait_free` | innodb는 페이지를 읽거나 만들기 전에 무료 페이지를 기다렸습니다.일반적으로 InnoDB 버퍼 풀에 쓰는 것은 백그라운드에서 발생합니다. 깨끗한 페이지를 사용할 수 없으면 더러운 페이지가 먼저 플러시되어 일부를 제거합니다. 이 작업이 완료되기까지 대기수를 계산합니다. 이 값이 작지 않은 경우 `innodb_buffer_pool_size`의 증가를 확인하십시오. |
| `Innodb_buffer_pool_write_requests` | InnoDB 버퍼 풀에 쓰는 요청 수. |
| `Innodb_buffered_aio_submitted` | |
| `Innodb_checkpoint_age` | Show Engine InnoDB 상태 출력의 로그 섹션에 표시된 바와 같이 체크 포인트 연령.(이것은 '로그 시퀀스 번호'에서 '마지막 체크 포인트'를 빼는 것과 같습니다.)|
| `Innodb_checkpoint_max_age` | Show Engine InnoDB 상태 출력의 로그 섹션에 표시된대로 Max Checkpoint Age.|
| `Innodb_data_fsyncs` | Innodb fsync (동기화) 호출 수.fsync 호출 주파수는 `innodb_flush_method` 구성 옵션의 영향을받을 수 있습니다.|
| `Innodb_data_pending_fsyncs` | 보류중인 innodb fsync (동기화) 호출 수fsync 호출 주파수는 `innodb_flush_method` 구성 옵션의 영향을받을 수 있습니다.|
| `Innodb_data_pending_reads` | 보류중인 innodb 읽기 수. |
| `Innodb_data_pending_writes` | 보류중인 innodb의 수. |
| `Innodb_data_read` | 서버 시작 이후로 읽은 InnoDB 바이트 수 (`InnoDB_Data_Reads`와 혼동되지 않음). |
| `Innodb_data_reads` | InnoDB 읽기 작업의 수 (`InnoDB_Data_Read`와 혼동되지 않음).|
| `Innodb_data_writes` | InnoDB 쓰기 작업의 수.|
| `Innodb_data_written` | 서버 시작 이후 작성된 InnoDB 바이트 수.|
| `Innodb_dblwr_pages_written` | InnoDB Doublewrite 버퍼에 기록 된 페이지 수.|
| `Innodb_dblwr_writes` | Innodb Doublewrite 버퍼에 대한 쓰기 수. |
| `Innodb_deadlocks` | Innodb 교착 상태의 총 수. 교착 상태는 적어도 두 번의 트랜잭션이 다른 거래가 완료되기를 기다리는 경우 발생하여 원형 의존성이 발생합니다. Innodb는 일반적으로 이것들을 빠르게 감지하여 오류를 반환합니다.|
| `Innodb_defragment_compression_failures` | 탈퇴 재 압축 실패의 수. innodb 테이블 스페이스의 해아를 참조하십시오.|
| `Innodb_defragment_count` | 해체 작업 수. innodb 테이블 스페이스의 해아를 참조하십시오.|
| `Innodb_defragment_failures` | 탈퇴 실패의 수. innodb 테이블 스페이스의 해아를 참조하십시오. |
| `Innodb_encryption_n_merge_blocks_decrypted` | |
| `Innodb_encryption_n_merge_blocks_encrypted` | |
| `Innodb_encryption_n_rowlog_blocks_decrypted` | |
| `Innodb_encryption_n_rowlog_blocks_encrypted` | |
| `Innodb_encryption_num_key_requests` | MariaDB 10.5.2에는 존재하지 않았습니다.|
| `Innodb_encryption_rotation_estimated_iops` | 테이블 및 테이블 스페이스 암호화를 참조하십시오. |
| `Innodb_encryption_rotation_pages_flushed` | 테이블 및 테이블 스페이스 암호화를 참조하십시오. |
| `Innodb_encryption_rotation_pages_modified` | 테이블 및 테이블 스페이스 암호화를 참조하십시오. |
| `Innodb_encryption_rotation_pages_read_from_cache` | 테이블 및 테이블 스페이스 암호화를 참조하십시오. |
| `Innodb_encryption_rotation_pages_read_from_disk` | 테이블 및 테이블 스페이스 암호화를 참조하십시오. |
| `Innodb_history_list_length` | 히스토리 목록 길이 Show Engine InnoDB 상태 출력의 트랜잭션 섹션에 표시된대로, xtradb는 mariadb 10.5.0에 도입 될 때까지만.|
| `Innodb_ibuf_discarded_delete_marks` | Show Engine InnoDB 상태 출력의 삽입 버퍼 및 적응 형 해시 인덱스 섹션에 표시된 바와 같이.|
| `Innodb_ibuf_discarded_deletes` | Show Engine InnoDB 상태 출력의 삽입 버퍼 및 적응 형 해시 인덱스 섹션에 표시된 바와 같이.|
| `Innodb_ibuf_discarded_inserts` | Show Engine InnoDB 상태 출력의 삽입 버퍼 및 적응 형 해시 인덱스 섹션에 표시된 바와 같이.|
| `Innodb_ibuf_free_list` | Show Engine InnoDB 상태 출력의 삽입 버퍼 및 적응 형 해시 인덱스 섹션에 표시된 바와 같이.|
| `Innodb_ibuf_merged_delete_marks` | Show Engine InnoDB 상태 출력의 삽입 버퍼 및 적응 형 해시 인덱스 섹션에 표시된 바와 같이.|
| `Innodb_ibuf_merged_deletes` | Show Engine InnoDB 상태 출력의 삽입 버퍼 및 적응 형 해시 인덱스 섹션에 표시된 바와 같이.|
| `Innodb_ibuf_merged_inserts` | Show Engine InnoDB 상태 출력의 삽입 버퍼 및 적응 형 해시 인덱스 섹션에 표시된 바와 같이.|
| `Innodb_ibuf_merges` | Show Engine InnoDB 상태 출력의 삽입 버퍼 및 적응 형 해시 인덱스 섹션에 표시된 바와 같이.|
| `Innodb_ibuf_segment_size` | Show Engine InnoDB 상태 출력의 삽입 버퍼 및 적응 형 해시 인덱스 섹션에 표시된 바와 같이.|
| `Innodb_ibuf_size` | Show Engine InnoDB 상태 출력의 삽입 버퍼 및 적응 형 해시 인덱스 섹션에 표시된 바와 같이.|
| `Innodb_instant_alter_column` | InnoDB의 Instant Add 열을 참조하십시오.|
| `Innodb_log_waits` | 로그 버퍼가 작아서 플러시 될 때까지 기다린 횟수. Redo log 경합비율은 ( `Innodb_log_waits` / `Innodb_log_writes` ) 로 계산할 수 있으며 1 미만값을 권장합니다. Wait이 높다면 `innodb_log_buffer_size`를 늘리는 것을 검토합니다.|
| `Innodb_log_write_requests` | innodb redo log에 쓰기 요청 수|
| `Innodb_log_writes` | innodb redo log file에 대한 물리적 쓰기 수 |
| `Innodb_lsn_current` | 로그 시퀀스 번호 Show Engine InnoDB 상태 출력의 로그 섹션에 표시된대로. |
| `Innodb_lsn_flushed` | Show Engine InnoDB 상태 출력의 로그 섹션에 표시된대로 로그 시퀀스 번호로 플러시됩니다. |
| `Innodb_lsn_last_checkpoint` | 로그 시퀀스 번호 Show Engine InnoDB 상태 출력의 로그 섹션에 표시된대로 마지막 체크 포인트.|
| `Innodb_master_thread_active_loops` | Show Engine InnoDB 상태 출력의 배경 스레드 섹션에 표시된대로. |
| `Innodb_master_thread_idle_loops` | Show Engine InnoDB 상태 출력의 배경 스레드 섹션에 표시된대로. |
| `Innodb_max_trx_id` | Show Engine InnoDB 상태 출력의 트랜잭션 섹션에 표시된대로. |
| `Innodb_mem_adaptive_hash` | Show Engine InnoDB 상태 출력의 버퍼 풀 및 메모리 섹션에 표시된 바와 같이. |
| `Innodb_mem_dictionary` | Show Engine InnoDB 상태 출력의 버퍼 풀 및 메모리 섹션에 표시된 바와 같이. |
| `Innodb_mem_total` | Show Engine InnoDB 상태 출력의 버퍼 풀 및 메모리 섹션에 표시된 바와 같이. |
| `Innodb_mutex_os_waits` | MUTEX OS는 Show Engine InnoDB 상태 출력의 세마포어 섹션에 표시된대로 대기합니다. |
| `Innodb_mutex_spin_rounds` | MUTEX SPIN ROUNDS SHOW Engine InnoDB 상태 출력의 세마포어 섹션에 표시됩니다. |
| `Innodb_mutex_spin_waits` | MUTEX 스핀은 Show Engine InnoDB 상태 출력의 세마포어 섹션에 표시된대로 대기합니다. |
| `Innodb_num_index_pages_written` | |
| `Innodb_num_non_index_pages_written` | |
| `Innodb_num_open_files` | InnoDB가 보유한 열린 파일 수. innodb 만. |
| `Innodb_num_page_compressed_trim_op` | 트림 작업 수.|
| `Innodb_num_page_compressed_trim_op_saved` | 이전 트림으로 인해 수행되지 않은 트림 작업 수입니다.|
| `Innodb_num_pages_decrypted` | 페이지 수 페이지는 해독되었습니다.테이블 및 테이블 스페이스 암호화를 참조하십시오.|
| `Innodb_num_pages_encrypted` | 페이지 수 페이지 암호화.테이블 및 테이블 스페이스 암호화를 참조하십시오. |
| `Innodb_num_pages_page_compressed` | 페이지가 압축 된 페이지 수입니다. |
| `Innodb_num_pages_page_compression_error` | 압축 오류 수.|
| `Innodb_num_pages_page_decompressed` | 압축 압축으로 압축 된 페이지 수. |
| `Innodb_oldest_view_low_limit_trx_id` | Show Engine InnoDB 상태 출력의 트랜잭션 섹션에 표시된대로. |
| `Innodb_onlineddl_pct_progress` | 장소 변경 테이블의 진행 상황을 보여줍니다.장소 대체가 디스크 및 버퍼 풀 상태에 크게 의존하기 때문에 정확하지 않을 수 있습니다. InnoDB에서 온라인 DDL의 모니터링 진행 및 시간 메모리 사용을 참조하십시오. |
| `Innodb_onlineddl_rowlog_pct_used` | 5 자리 정수에서 행 로그 버퍼 사용량을 보여줍니다.(10000은 100.00%) InnoDB에서 온라인 DDL의 모니터링 진행 및 시간 메모리 사용을 참조하십시오.|
| `Innodb_onlineddl_rowlog_rows` | 행 로그 버퍼에 저장된 행 수. InnoDB에서 온라인 DDL의 모니터링 진행 및 시간 메모리 사용을 참조하십시오. |
| `Innodb_os_log_fsyncs` | InnoDB log fsync (sync-to-disk) 요청 수|
| `Innodb_os_log_pending_fsyncs` | 보류중인 innodb log fsync (sync-to-disk) 요청 수 |
| `Innodb_os_log_pending_writes` | 보류중인 innodb 로그의 수.|
| `Innodb_os_log_written` | InnoDB 로그에 기록 된 바이트 수.|
| `Innodb_page_compression_saved` | 페이지 압축으로 저장된 바이트 수. |
| `Innodb_page_compression_trim_sect1024` | 1K 블록 크기에 대한 페이지 압축/NVM 압축 워크로드에 대해 수행 된 트림 작업 수. |
| `Innodb_page_compression_trim_sect16384` | 16K 블록 크기에 대한 페이지 압축/NVM 압축 워크로드에 대해 수행 된 트림 작업 수.|
| `Innodb_page_compression_trim_sect2048` | 2K 블록 크기에 대한 페이지 압축/NVM 압축 워크로드에 대해 수행 된 트림 작업 수. |
| `Innodb_page_compression_trim_sect32768` | 32k 블록 크기에 대한 페이지 압축/NVM 압축 워크로드에 대해 수행 된 트림 작업 수.|
| `Innodb_page_compression_trim_sect4096` | 4K 블록 크기에 대한 페이지 압축/NVM 압축 워크로드에 대해 수행 된 트림 작업 수. |
| `Innodb_page_compression_trim_sect512` | 512 바이트 블록 크기에 대한 페이지 압축/NVM 압축 워크로드에 대한 트림 작업 수. |
| `Innodb_page_compression_trim_sect8192` | 8K 블록 크기에 대한 페이지 압축/NVM 압축 워크로드에 대해 수행 된 트림 작업 수. |
| `Innodb_page_size` | innodb에서 사용하는 페이지 크기.기본값은 16KB로, 다른 값으로 컴파일 할 수 있습니다. |
| `Innodb_pages_created` | 생성 된 InnoDB 페이지 수.|
| `Innodb_pages_read` | innodb 페이지의 수를 읽습니다.|
| `Innodb_pages_written` | innodb 페이지의 수.|
| `Innodb_pages0_read` | InnoDB에 대한 데이터 암호화의 원래 구현이 페이지를 읽기위한 새로운 코드 경로를 도입했기 때문에 InnoDB 데이터 파일의 첫 페이지를 추적하는 데 반대합니다. 첫 번째 페이지의 추가 판독 값이 제거 될 때 MariaDB 10.4.0에서 제거되었고 암호화 서브 시스템은 `FIL_NODE_OPEN_FILE ()`에서 각 데이터 파일의 첫 페이지를 처음 읽을 때마다 초기화됩니다. |
| `Innodb_purge_trx_id` | Purge Transaction ID Show Engine InnoDB 상태 출력의 트랜잭션 섹션에 표시된대로.|
| `Innodb_purge_undo_no` | Show Engine InnoDB 상태 출력의 트랜잭션 섹션에 표시된대로. |
| `Innodb_read_views_memory` | Show Engine InnoDB 상태 출력의 버퍼 풀 및 메모리 섹션에 표시된 바와 같이. InnoDB 읽기보기에 할당 된 바이트의 메모리의 총계를 보여줍니다. |
| `Innodb_row_lock_current_waits` | 보류중인 행 잠금 수는 innodb 테이블에서 대기합니다.|
| `Innodb_row_lock_time_avg` | 밀리 초의 평균 시간은 innodb 행 잠금을 얻는 데 소비되었습니다. |
| `Innodb_row_lock_time_max` | 밀리 초의 최대 시간은 Innodb 행 잠금 장치를 얻는 데 소비되었습니다.|
| `Innodb_rows_deleted` | InnoDB 테이블에서 삭제 된 행 수.|
| `Innodb_rows_inserted` | InnoDB 테이블에 삽입 된 행 수. |
| `Innodb_rows_updated` | InnoDB 테이블로 업데이트 된 행 수입니다.|
| `Innodb_s_lock_os_waits` | Show Engine InnoDB 상태 출력의 세마포어 섹션에 표시된 바와 같이. |
| `Innodb_s_lock_spin_rounds` | Show Engine InnoDB 상태 출력의 세마포어 섹션에 표시된 바와 같이. |
| `Innodb_s_lock_spin_waits` | Show Engine InnoDB 상태 출력의 세마포어 섹션에 표시된 바와 같이. |
| `Innodb_scrub_background_page_reorganizations` | 테이블 및 테이블 스페이스 암호화를 참조하십시오. |
| `Innodb_scrub_background_page_split_failures_missing_index` | 테이블 및 테이블 스페이스 암호화를 참조하십시오. |
| `Innodb_scrub_background_page_split_failures_out_of_filespace` | 테이블 및 테이블 스페이스 암호화를 참조하십시오. |
| `Innodb_scrub_background_page_split_failures_underflow` | 테이블 및 테이블 스페이스 암호화를 참조하십시오. |
| `Innodb_scrub_background_page_split_failures_unknown` | 테이블 및 테이블 스페이스 암호화를 참조하십시오. |
| `Innodb_scrub_background_page_splits` | 테이블 및 테이블 스페이스 암호화를 참조하십시오. |
| `Innodb_scrub_log` | |
| `Innodb_secondary_index_triggered_cluster_reads` | 접두사 인덱스 쿼리 최적화 (MDEV-6929)의 효과를 추적하는 데 사용됩니다.최적화가 항상 활성화됨에 따라 MariaDB 10.10에서 제거되었습니다.|
| `Innodb_secondary_index_triggered_cluster_reads_avoided` | 접두사 인덱스 쿼리 최적화 (MDEV-6929)의 효과를 추적하는 데 사용됩니다.최적화가 항상 활성화됨에 따라 MariaDB 10.10에서 제거되었습니다.|
| `Innodb_system_rows_deleted` | |
| `Innodb_system_rows_inserted` | |
| `Innodb_system_rows_read` | |
| `Innodb_system_rows_updated` | |
| `Innodb_truncated_status_writes` | Show Engine InnoDB 상태에서 출력 횟수가 잘립니다.|
| `Innodb_undo_truncations` | 실행 취소 테이블 스페이스 잘림 작업 수. |
| `Innodb_x_lock_os_waits` | Show Engine InnoDB 상태 출력의 세마포어 섹션에 표시된 바와 같이. |
| `Innodb_x_lock_spin_rounds` | Show Engine InnoDB 상태 출력의 세마포어 섹션에 표시된 바와 같이. |
| `Innodb_x_lock_spin_waits` | Show Engine InnoDB 상태 출력의 세마포어 섹션에 표시된 바와 같이. |
| `Binlog_cache_disk_use` | 일반적인 바이너리 로그 캐시에 맞지 않기 때문에 임시 디스크 캐시를 사용한 트랜잭션 수는 `binlog_cache_size`보다 큽니다. 글로벌 값은 플러시 상태로 플러시 될 수 있습니다.|
| `Binlog_cache_use` | `Binlog_cache_size` 보다 작은 일반 바이너리 로그 캐시를 사용한 트랜잭션 수.글로벌 값은 플러시 상태로 플러시 될 수 있습니다. |
| `Binlog_group_commit_trigger_count` | 그룹의 이진 로그 커밋 수가 변수 `binlog_commit_wait_count`에 의해 설정된 한계에 도달하여 총 그룹 커밋 수가 트리거됩니다. 이진 로그는 그룹 커밋을 참조하십시오.|
| `Binlog_group_commit_trigger_lock_wait` | 이전 바이너리 로그 커밋으로 잠금 장치가 보유 된 잠금 대기로 인해 이진 로그 커밋이 지연 되었기 때문에 총 그룹 커밋이 트리거되었습니다. 이런 일이 발생하면 이후의 이진 로그 커밋이 다음 그룹 커밋에 배치됩니다. 이진 로그는 그룹 커밋을 참조하십시오. |
| `Binlog_group_commit_trigger_timeout` | 첫 번째 바이너리 로그 커밋이 변수 binlog_commit_wait_usec에 의해 설정된 한계에 도달 한 이후의 시간으로 인해 총 그룹 커밋이 트리거되었습니다.이진 로그는 그룹 커밋을 참조하십시오.|
| `Binlog_group_commits` | 이진 로그에 수행 된 총 그룹 커밋 수.이진 로그는 그룹 커밋을 참조하십시오. |
| `Binlog_snapshot_position` | 이진 로그 위치. Show Master 상태와 달리 스냅 샷을 찍은 이후 다른 거래가 커밋 된 것에 관계없이 거래 적으로 일관된 방식으로 쿼리 할 수 있습니다.일관된 스냅 샷으로 시작 트랜잭션 향상을 참조하십시오.|
| `Binlog_stmt_cache_disk_use` | 일반적인 바이너리 로그 캐시에 맞지 않기 때문에 임시 디스크 캐시를 사용한 비 변환 문의 수는 `binlog_stmt_cache_size` 보다 큽니다.글로벌 값은 플러시 상태로 플러시 될 수 있습니다.|
| `Binlog_stmt_cache_use` | `BINLOG_STMT_CACHE_SIZE` 보다 작은 일반 바이너리 로그 캐시를 사용한 비 변환 문의 수.글로벌 값은 플러시 상태로 플러시 될 수 있습니다. |
| `Com_change_master` | 실행된 진술에 대한 변경 마스터 수. |
| `Com_show_master_status` | 실행된 쇼 마스터 상태 명령 수. |
| `Com_show_new_master` | |
| `Com_show_slave_hosts` | 실행된 쇼 슬레이브 호스트 명령 수. |
| `Com_show_slave_status` | 실행된 쇼 슬레이브 상태 명령 수.|
| `Com_slave_start` | 실행된 START 슬레이브 명령 수. mariadb 10.0에서 제거하면 `com_start_slave`를 참조하십시오. |
| `Com_slave_stop` | STOP 슬레이브 명령 수 실행. mariadb 10.0에서 제거하면 `com_stop_slave`를 참조하십시오. |
| `Com_start_all_slaves` | 실행된 모든 노예 명령을 시작합니다. |
| `Com_start_slave` | 실행된 START 슬레이브 명령 수.이전 `com_slave_start`를 대체합니다. |
| `Com_stop_all_slaves` | 모든 노예 명령이 실행된 중지 수.|
| `Com_stop_slave` | STOP 슬레이브 명령 수 실행.이전 `com_slave_stop`을 대체합니다. |
| `Handler_commit` | 내부 커밋 요청 수. `COM_COMMIT`와는 다릅니다. |
| `Handler_delete` | 테이블에서 행이 삭제되었습니다.삭제 명령문을 계산하는 `com_delete`와 다릅니다. |
| `Handler_discover` | Discovery는 서버가 NDBCluster 스토리지 엔진에 주어진 이름이있는 테이블에 대해 알고 있는지 요청할 때입니다. `handler_discover`는 테이블이 이런 식으로 발견 된 횟수를 나타냅니다.|
| `Handler_external_lock` | `external_lock ()` 함수에 대한 각 호출에 대해 점점 씩 증가하며 일반적으로 테이블 인스턴스에 대한 액세스의 시작 및 끝에서 발생합니다.|
| `Handler_icp_attempts` | 푸시 된 인덱스 조건이 점검되었습니다. `handler_ICP_ATTEMPTS`의 비율이 작을수록 필터링이 더 좋습니다. 인덱스 조건 푸시 다운을 참조하십시오. |
| `Handler_icp_match` | 푸시 된 인덱스 조건이 일치했습니다. `handler_ICP_ATTEMPTS`의 비율이 작을수록 필터링이 더 좋습니다. 인덱스 조건 푸시 다운을 참조하십시오.|
| `Handler_mrr_init` | 얼마나 많은 MRR (다중 범위 읽기) 스캔 수를 계산합니다.멀티 범위 읽기 최적화를 참조하십시오. |
| `Handler_mrr_key_refills` | 키 버퍼가 리필되었다.(초기 충전을 계산하지 않음) 0이 아닌 값은 한 번에 키 정렬 및 스위프 패스를 수행 할 메모리가 충분하지 않음을 나타냅니다.멀티 범위 읽기 최적화를 참조하십시오.|
| `Handler_mrr_rowid_refills` | Rowid 버퍼가 리필되었다.(초기 충전을 계산하지 않음) 0이 아닌 값은 한 번에 Rowid Sort-and-Sweep 패스를 수행 할 메모리가 충분하지 않음을 나타냅니다. 멀티 범위 읽기 최적화를 참조하십시오.|
| `Handler_prepare` | 2 단계 커밋의 수. |
| `Handler_read_first` | 인덱스에서 첫 번째 행을 읽는 요청 수. 높은 값은 많은 전체 인덱스 스캔을 나타냅니다. 예, a는 a가 색인화 된 열인 Table_name에서 a를 선택하십시오.|
| `Handler_read_key` | 인덱스 값을 기준으로 행 읽기 요청 수. 높은 값은 인덱스가 정기적으로 사용되고 있음을 나타냅니다. 이는 일반적으로 양수입니다.|
| `Handler_read_last` | 인덱스에서 마지막 행을 읽는 요청 수. DESC로 주문하면 마지막 키 요청과 몇 가지 이전 키 요청이 이어집니다.|
| `Handler_read_next` | 인덱스에서 다음 행을 읽는 요청 수(순서대로). 인덱스 스캔을 수행하거나 범위 구속 조건으로 인덱스 열을 쿼리 할 때 증가합니다.|
| `Handler_read_prev` | 인덱스에서 이전 행을 읽는 요청 수(순서대로). 대부분 Desc에 의해 주문과 함께 사용됩니다. |
| `Handler_read_retry` | `semi_consistent_read` (innodb 기능)에 의해 트리거 된 읽기 재 시도 수. |
| `Handler_read_rnd` | 위치에 따라 행을 읽으라는 요청 수. 이 값이 높으면 인덱스를 제대로 사용하지 않는 조인을 사용하지 않거나 많은 전체 테이블 스캔을 수행 할 수 있습니다. |
| `Handler_read_rnd_deleted` | 위치에 따라 행을 삭제하는 요청 수.|
| `Handler_read_rnd_next` | 다음 행을 읽는 요청 수. 이들 중 다수는 많은 테이블 스캔과 부적절하게 사용 된 인덱스를 나타낼 수 있습니다. |
| `Handler_rollback` | 스토리지 엔진에 제공되는 트랜잭션 롤백 요청 수. 실행된 롤백 명령 수인 `com_rollback`과 다릅니다.|
| `Handler_savepoint` | 트랜잭션 수 SavePoint 생성 요청 수. 실행된 SavePoint 명령의 수인 `COM_SAVEPOINT`와 다릅니다. |
| `Handler_savepoint_rollback` | 트랜잭션 저장 포인트로의 롤백 요청 수.|
| `Handler_tmp_delete` | 임시 테이블에서 행을 삭제하는 요청 수.|
| `Handler_tmp_update` | 행을 임시 테이블로 업데이트하라는 요청 수.|
| `Handler_tmp_write` | 임시 테이블에 행을 작성하라는 요청 수.|
| `Handler_update` | 테이블에서 행을 업데이트하라는 요청 수. mariadb 5.3이므로 더 이상 임시 테이블을 계산하지 않습니다. `handler_tmp_update`를 참조하십시오.|
| `Handler_write` | 테이블에 행을 작성하라는 요청 수. mariadb 5.3이므로 더 이상 임시 테이블을 계산하지 않습니다. `handler_tmp_write`를 참조하십시오.|
| `Master_gtid_wait_count` | `Master_GTID_WAIT`가 호출되는 횟수.|
| `Master_gtid_wait_time` | `Master_GTID_WAIT`에서 보낸 총 시간. |
| `Master_gtid_wait_timeouts` | `master_gtid_wait`에서 발생하는 시간 초과 수.|
| `Rpl_transactions_multi_engine` | `mysql.gtid_slave_pos`의 업데이트를 고려하기 전에 다중 (트랜잭션) 스토리지 엔진의 변경과 관련된 복제 된 트랜잭션의 수. 이들은 복제에 의해 도입 된 GTID 위치 업데이트와 무관 한 이미 교차 엔진 인 거래입니다. 글로벌 값은 플러시 상태로 플러시 될 수 있습니다.|
| `Slave_connections` | `register_slave` 시도의 수. 실제로 노예가 마스터와 연결하려고 시도한 횟수. |
| `Slave_open_temp_tables` | 노예가 열린 임시 테이블 수.|
| `Slave_received_heartbeats` | 노예가 주인으로부터받은 하트 비트 수. |
| `Slave_retried_transactions` | 서버가 시작된 이래로 슬레이브가 거래를 재고 한 횟수. 글로벌 값은 플러시 상태로 플러시 될 수 있습니다.|
| `Slave_skipped_errors` | 슬레이브가 슬레이브 스크립 오류로 정의 된 오류를 건너 뜁니다. |
| `Slaves_connected` | 연결된 노예 수. |
| `Syncs` | `my_sync()`가 호출 된 횟수 또는 서버가 데이터를 디스크로 강제 해야하는 횟수. 이진 로그, .frm 생성을 포함합니다. |
| `Table_locks_immediate` | 즉시 완료된 테이블 잠금 수.글로벌 값은 플러시 상태로 플러시 될 수 있습니다.|
| `Table_locks_waited` | 테이블 잠금에 대한 요청을 즉시 승인 할 수 없어 대기가 필요한 횟수. 이 값이 높고 성능 문제가 있는 경우에는 먼저 쿼리를 최적화한 다음 테이블을 분할하거나 복제를 사용해야 합니다.|
| `Table_open_cache_active_instances` | 열린 테이블 캐시 조회의 활성 인스턴스 수.|
| `Table_open_cache_hits` | 열린 테이블 캐시 조회에 대한 히트 수.|
| `Table_open_cache_misses` | 열린 테이블 캐시 조회 용 미스 수.|
| `Table_open_cache_overflows` | 열린 테이블 캐시 조회 용 오버 플로우 수.|
| `Threads_cached` | 스레드 캐시에 캐시 된 스레드 수. 스레드 풀이 사용되는 경우이 값은 0이됩니다.|
| `Threads_created` | 클라이언트 연결에 응답하기 위해 생성 된 스레드 수. 너무 크면 `thread_cache_size` 증가를 보십시오. |
| `Transactions_gtid_foreign_engine` | `GTID_SLAVE_POS` 테이블의 업데이트가 거래에 참여하지 않은 스토리지 엔진을 선택 해야하는 복제 트랜잭션 수입니다. 이것은 `GTID_POS_AUTO_ENGINES`를 설정하는 것이 유용할 수 있음을 나타낼 수 있습니다. 글로벌 값은 플러시 상태로 플러시 될 수 있습니다. |
| `Transactions_multi_engine` | 다중 (트랜잭션) 스토리지 엔진에서 데이터를 변경 한 트랜잭션 수. 이것이 `rpl_transactions_multi_engine`보다 상당히 큰 경우 `GTID_POS_AUTO_ENGINES`를 설정하면 교차 엔진 트랜잭션의 필요성을 줄일 수 있음을 나타냅니다. 글로벌 값은 플러시 상태로 플러시 될 수 있습니다.|
| `Update_scan` | 전체 테이블 스캔이 필요한 업데이트 수.|
| `cpu_idle(xos)` | cpu프로세서 유휴시간|
| `cpu_iowait(xos)` | cpu가 IO를 대기한 시간 |
| `cpu_sys(xos)` | cpu가 kernel code 를 수행한 시간 |
| `cpu_user(xos)` | cpu가 사용자 코드를 실행하는데 소요한 시간 |
| `cpu(xos)` | `cpu_user` + `cpu_sys` + `cpu_iowait` |
| `disk_read_bytes(xos)` | 읽기 작업 동안 디스크에서 전송되는 바이트 |
| `disk_reads(xos)` | |
| `disk_write_bytes(xos)` | 쓰기 작업 동안 디스크에 전송되는 바이트|
| `disk_writes(xos)` | |
| `mem_available(xos)` | 메모리 가용영역|
| `mem_buffcache(xos)` | |
| `mem_free(xos)` | 시스템에서 사용하고 있지 않은 메모리양 |
| `mem_swapfree(xos)` | swap 사용하지 않은 영역. Swap영역은 디스크 공간에 확보되기 때문에 swap을 사용하고 있다면 시스템 성능이 급격하게 떨어질 수 있다. |
| `mem_swaptotal(xos)` | swap 전체영역. 물리메모리가 부족한 경우를 대비하여 설정함|
| `mem_total(xos)` | 전체 메모리양 |
| `mem_used(xos)` | 시스템에서 사용하고 있는 메모리양|
| `mem(xos)` | |
| `net_recv_bytes(xos)` | |
| `net_recv_packets(xos)` | |
| `net_send_bytes(xos)` | |
| `net_send_packets(xos)` | |
| `Com_admin_commands` | 실행된 관리 명령 수. 여기에는 테이블 덤프, 변경 사용자, 이진 로그 덤프, 셧다운, 핑 및 디버그가 포함됩니다.|
| `Com_alter_db` | 실행된 Alter 데이터베이스 명령 수. |
| `Com_alter_db_upgrade` | Alter 데이터베이스 수. 업그레이드 명령이 실행되었습니다. |
| `Com_alter_event` | 실행된 Alter 이벤트 명령 수.|
| `Com_alter_function` | 실행된 ALTER 기능 명령 수. |
| `Com_alter_procedure` | 실행된 변경 절차 명령 수.|
| `Com_alter_sequence` | 실행된 변경 시퀀스 명령 수. |
| `Com_alter_server` | 실행된 변경 서버 명령 수.|
| `Com_alter_table` | 실행된 알터 테이블 명령 수. |
| `Com_alter_tablespace` | 실행된 ALTER 테이블 스페이스 명령 수(MariaDB에 의해 지원되지 않음). |
| `Com_alter_user` | 실행된 변경 사용자 명령 수. |
| `Com_analyze` | 실행된 분석 명령 수. |
| `Com_assign_to_keycache` | 실행된 KeyCache 명령에 지정된 수.|
| `Com_backup_table` | Mariadb 5.5에서 제거.이전 버전에서는 `com_backup_table`에는 실행된 백업 테이블 명령 수가 포함되어 있습니다. |
| `Com_begin` | 실행된 시작 또는 시작 트랜잭션 명세서 수. |
| `Com_binlog` | 실행된 Binlog 명령 수. |
| `Com_call_procedure` | 통화 `reverure_name` 문의 수정. |
| `Com_change_db` | 사용 된 `Database_Name` 명령 수는 실행되었습니다. |
| `Com_check` | 실행된 체크 테이블 명령 수. |
| `Com_checksum` | 체크섬 테이블 명령 수 실행.|
| `Com_compound_sql` | 화합물 SQL 문의 수. |
| `Com_create_db` | 실행된 데이터베이스 명령 수|
| `Com_create_event` | 실행된 이벤트 명령 수이벤트가 실행될 때가 아니라 Create Event가 실행될 때 증가한다는 점에서 `Executed_events`와 다릅니다. |
| `Com_create_function` | 실행된 기능 명령 수|
| `Com_create_index` | 실행된 인덱스 명령 수 |
| `Com_create_package` | |
| `Com_create_package_body` | |
| `Com_create_procedure` | 생성 프로 시저 명령 수 실행. |
| `Com_create_role` | 실행된 역할 명령 수|
| `Com_create_sequence` | 실행된 시퀀스 명령 수 |
| `Com_create_server` | 실행된 서버 명령 수|
| `Com_create_table` | 생성 테이블 명령 수 실행. |
| `Com_create_temporary_table` | 실행된 임시 테이블 명령 수|
| `Com_create_trigger` | 실행된 트리거 명령 수 |
| `Com_create_udf` | 실행된 UDF 명령 수 |
| `Com_create_user` | 실행된 사용자 명령 수 |
| `Com_create_view` | 실행된 뷰 명령 수 |
| `Com_dealloc_sql` | 실행된 거래 명령 수. |
| `Com_delete_multi` | 실행된 다중 테이블 삭제 명령 수.|
| `Com_do` | 실행된 DO 명령 수. |
| `Com_drop_db` | 실행된 드롭 데이터베이스 명령 수 |
| `Com_drop_event` | 실행된 드롭 이벤트 명령 수. |
| `Com_drop_function` | 실행된 드롭 함수 명령 수.|
| `Com_drop_index` | 실행된 드롭 인덱스 명령 수. |
| `Com_drop_package` | |
| `Com_drop_package_body` | |
| `Com_drop_procedure` | 낙하 절차 명령 수 실행.|
| `Com_drop_role` | 실행된 드롭 역할 명령 수.|
| `Com_drop_sequence` | 실행된 드롭 시퀀스 명령 수. |
| `Com_drop_server` | 실행된 드롭 서버 명령 수.|
| `Com_drop_table` | 실행된 드롭 테이블 명령 수. |
| `Com_drop_temporary_table` | 실행된 드롭 임시 테이블 명령 수.|
| `Com_drop_trigger` | 실행된 드롭 트리거 명령 수. |
| `Com_drop_user` | 실행된 드롭 사용자 명령 수. |
| `Com_drop_view` | 실행된 드롭 뷰 명령 수. |
| `Com_empty_query` | SQL 쿼리를 생성하지 않는 서버에 대한 쿼리 수. 결과를 반환하는 SQL 쿼리는 `com_empty_query`를 증가시키지 않습니다.서버로 전송 된 빈 쿼리의 예는 `mysql-comments -e '-SQL Comment'`입니다. |
| `Com_execute_immediate` | 실행된 즉각적인 진술을 실행하는 수. |
| `Com_execute_sql` | 실행된 실행 진술 수. |
| `Com_flush` | 실행된 플러시 명령 수.이는 `Flush_Commands`와 다르며 내부 서버 플러시 요청도 계산합니다. |
| `Com_get_diagnostics` | 실행된 GET 진단 명령 수. |
| `Com_grant` | 실행된 보조금 명령 수.|
| `Com_grant_role` | 실행된 보조금 역할 명령 수. |
| `Com_ha_close` | 핸들러 수 `table_name` 클로즈 명령 실행. |
| `Com_ha_open` | 핸들러 수 `table_name` 오픈 명령이 실행되었습니다.|
| `Com_ha_read` | 핸들러 수 `table_name` 읽기 명령이 실행되었습니다.|
| `Com_help` | 실행된 도움말 명령 수.|
| `Com_insert_select` | 삽입 수. 실행된 명령을 선택합니다.|
| `Com_install_plugin` | 실행된 설치 플러그인 명령 수 |
| `Com_kill` | 처형 명령 수.|
| `Com_load` | 실행된로드 명령 수.|
| `Com_load_master_data` | |
| `Com_load_master_table` | |
| `Com_lock_tables` | \[잠금 테이블 \| 잠금 테이블\] 명령이 실행된 명령. |
| `Com_multi` | |
| `Com_optimize` | 실행된 최적화 명령 수 |
| `Com_preload_keys` | |
| `Com_prepare_sql` | 실행된 준비 진술 수. |
| `Com_purge` | 실행된 퍼지 명령 수. |
| `Com_purge_before_date` | 명령이 실행되기 전에 퍼지 수. |
| `Com_release_savepoint` | 실행된 릴리스 저장 포인트 명령 수. |
| `Com_rename_table` | 실행된 이름 바꾸기 테이블 명령 수입니다.|
| `Com_rename_user` | 실행된 사용자 명령 이름 바꾸기 수|
| `Com_repair` | 수리 테이블 명령 수 실행. |
| `Com_replace` | 실행된 교체 명령 수. |
| `Com_replace_select` | 대체 수. 선택된 명령.|
| `Com_reset` | 재설정 명령 수. |
| `Com_resignal` | 실행된 사임 명령 수. |
| `Com_restore_table` | Mariadb 5.5에서 제거.이전 버전에서는 `com_restore_table`에는 실행된 복원 테이블 명령 수가 포함되어 있습니다.|
| `Com_revoke` | 실행된 취소 명령 수. |
| `Com_revoke_all` | 실행된 모든 명령의 취소 수. |
| `Com_rollback_to_savepoint` | 롤백 수. SavePoint 명령을 실행했습니다.|
| `Com_savepoint` | 실행된 SavePoint 명령 수. 트랜잭션 저장 포인트 생성 요청 수인 `handler_savepoint`와 다릅니다. |
| `Com_set_option` | 실행된 설정 옵션 명령 수 |
| `Com_show_authors` | 실행된 쇼 저자 명령 수. |
| `Com_show_binlog_events` | 실행된 쇼 Binlog 이벤트 명세서 수.|
| `Com_show_binlogs` | 실행된 쇼 바이너리 로그 명령 수.|
| `Com_show_charsets` | 실행된 쇼 캐릭터 세트 명령 수. |
| `Com_show_collations` | 실행된 쇼 Collation 명령 수.|
| `Com_show_column_types` | |
| `Com_show_contributors` | Show Contributors 명령 수 실행.|
| `Com_show_create_db` | 쇼 수는 실행된 데이터베이스 명령을 작성합니다. |
| `Com_show_create_event` | 쇼 수는 실행된 이벤트 명령을 작성합니다.|
| `Com_show_create_func` | 쇼 수는 실행된 함수 명령을 작성합니다. |
| `Com_show_create_package` | |
| `Com_show_create_package_body` | |
| `Com_show_create_proc` | 쇼의 수용 프로 시저 명령을 실행했습니다. |
| `Com_show_create_table` | 쇼의 수용 테이블 명령을 실행했습니다. |
| `Com_show_create_trigger` | 쇼 수는 실행된 트리거 명령을 만듭니다. |
| `Com_show_create_user` | 쇼 수는 실행된 사용자 명령을 작성합니다.|
| `Com_show_databases` | 실행된 쇼 데이터베이스 명령 수. |
| `Com_show_engine_logs` | 실행된 쇼 엔진 로그 명령 수.|
| `Com_show_engine_mutex` | 실행된 Show Engine Mutex 명령 수.|
| `Com_show_engine_status` | 실행된 쇼 엔진 상태 명령 수.|
| `Com_show_errors` | 실행된 쇼 오류 명령 수. |
| `Com_show_events` | 실행된 쇼 이벤트 명령 수.|
| `Com_show_explain` | 쇼의 수는 실행된 명령을 설명합니다. |
| `Com_show_fields` | 실행된 쇼 열 또는 쇼 필드 명령 수.|
| `Com_show_function_status` | 실행된 쇼 기능 상태 명령 수.|
| `Com_show_generic` | `show index_statistics` 및 `show table_statistics`와 같이 실행된 일반 쇼 명령 수|
| `Com_show_grants` | Show Grants 명령 수에 실행되었습니다.|
| `Com_show_keys` | 쇼 인덱스 수 또는 쇼 키 명령 수는 실행되었습니다. |
| `Com_show_open_tables` | 실행된 쇼 열기 테이블 명령 수. |
| `Com_show_package_body_status` | |
| `Com_show_package_status` | |
| `Com_show_plugins` | 실행된 쇼 플러그인 명령 수. |
| `Com_show_privileges` | 실행된 쇼 권한 명령 수. |
| `Com_show_procedure_status` | 실행된 쇼 절차 상태 명령 수.|
| `Com_show_processlist` | 실행된 Show ProcessList 명령 수. |
| `Com_show_profile` | 실행된 쇼 프로필 명령 수.|
| `Com_show_profiles` | 실행된 쇼 트레이스 명령 수. |
| `Com_show_relaylog_events` | Show RelayLog 이벤트 명령 수 실행.|
| `Com_show_status` | 실행된 쇼 상태 명령 수. |
| `Com_show_storage_engines` | 쇼 스토리지 엔진 수 - 또는 쇼 엔진 - 실행된 명령.|
| `Com_show_table_status` | 실행된 쇼 테이블 상태 명령 수. |
| `Com_show_tables` | 실행된 쇼 테이블 명령 수.|
| `Com_show_triggers` | 실행된 쇼 트리거 명령 수.|
| `Com_show_warnings` | 실행된 쇼 경고 명령 수. |
| `Com_shutdown` | 셧다운 명령 수. |
| `Com_signal` | 실행된 신호 문의 수. |
| `Com_stmt_close` | 준비된 진술 수가 닫혔습니다.(거래 또는 삭제) |
| `Com_stmt_execute` | 준비된 진술의 수.|
| `Com_stmt_fetch` | 준비된 진술의 수.|
| `Com_stmt_prepare` | 준비된 진술의 수.|
| `Com_stmt_reprepare` | 준비된 진술의 수는 반복됩니다. |
| `Com_stmt_reset` | 긴 데이터를 전송하여 덩어리에 축적 된 준비된 명세서의 데이터가 재설정 된 준비된 진술의 수.|
| `Com_stmt_send_long_data` | 매개 변수 데이터가 청크로 전송 된 준비된 명령문 수(긴 데이터)|
| `Com_truncate` | 실행된 절단 명령 수. |
| `Com_uninstall_plugin` | 실행되지 않은 플러그인 명령 수.|
| `Com_unlock_tables` | 실행된 잠금 해제 테이블 명령 수.|
| `Com_update_multi` | 실행된 다중 테이블 업데이트 명령 수.|
| `Com_xa_commit` | 커밋 된 XA 진술 수. |
| `Com_xa_end` | XA 문의 수가 끝났습니다. |
| `Com_xa_prepare` | 준비된 XA 문의 수.|
| `Com_xa_recover` | 실행된 XA 복구 명령문의 수.|
| `Com_xa_rollback` | XA 문의 수는 롤백되었습니다. |
| `Com_xa_start` | XA 진술 수가 시작되었습니다. |
| `Rows_read` | 행을 읽는 요청 수 (임시 테이블 제외). |
| `Rows_sent` | |
| `Rows_tmp_read` | 임시 테이블에서 행을 읽으라는 요청 수.|
| `Select_full_join` | 하나 이상의 테이블 조인 시 인덱스를 사용하지 않고 드리븐(driven) 테이블을 풀 스캔(full scan)한 횟수 |
| `Select_full_range_join` | 하나 이상의 테이블 조인 시 드리븐(driven) 테이블을 레인지 스캔(range scan)한 횟수 |
| `Select_range` | 드라이빙(driving) 테이블을 레인지스캔(range scan)한 횟수. 실행계획의 Type컬럼에 Range가 표시됩니다. |
| `Select_range_check` | 각 행 후 키 사용량을 확인하는 키가없는 조인 수. 0이 아닌 경우 테이블 인덱스를 확인해야 할 수도 있습니다. |
| `Select_scan` | 드라이빙(driving) 테이블을 풀 스캔(full scan)한 횟수. 이 경우 실행계획의 type컬럼에 ALL이 표시됩니다.|
| `Slow_launch_threads` | `slow_launch_time` 보다 오래 걸리는 스레드 수. 글로벌 값은 플러시 상태로 플러시 될 수 있습니다.|
| `Sort_merge_passes` | 정렬 알고리즘에 의해 수행되는 병합 패스 수.너무 높으면 쿼리 색인을 개선하거나 `Sort_Buffer_Size`를 늘리는 것을 살펴 봐야 할 수도 있습니다.|
| `Sort_priority_queue_sorts` | 분류의 횟수는 우선 순위 대기열을 통해 수행되었습니다.(정렬이 완료된 총 횟수는 `SOM SORT_RANGE` 및 `SORT_SCAN`)입니다) 작은 한계 최적화가있는 Filesort를 참조하십시오. |
| `Sort_range` | 범위를 사용한 종류의 종류. |
| `Sort_scan` | 전체 테이블 스캔을 사용한 종류의 종류.|
| `Subquery_cache_hit` | 모든 하위 쿼리 캐시 히트에 대한 카운터.글로벌 값은 플러시 상태로 플러시 될 수 있습니다. |
| `Subquery_cache_miss` | 모든 하위 쿼리 캐시 미스에 대한 카운터.글로벌 값은 플러시 상태로 플러시 될 수 있습니다. |
| `Acl_column_grants` | 부여 된 열 권한 수 (`mysql.columns_priv` 테이블의 행).|
| `Acl_database_grants` | 부여 된 데이터베이스 권한 수 (`MySQL.db` 테이블의 행). |
| `Acl_function_grants` | 부여 된 기능 권한 수 (`MySQL.procs_priv` 테이블의 행은 일상적인 유형의 함수 유형). |
| `Acl_package_body_grants` | |
| `Acl_package_spec_grants` | |
| `Acl_procedure_grants` | 부여 된 절차 권한 수 (`MySQL.procs_priv` 테이블의 행은 일상적인 유형의 절차를 가진 행).|
| `Acl_proxy_users` | 부여 된 프록시 권한 수 (`mysql.proxies_priv` 테이블의 행).|
| `Acl_role_grants` | 부여 된 역할 권한 수 (`mysql.roles_mapping` 테이블의 행).|
| `Acl_roles` | 역할 수 (`mysql.user` 테이블의 행은 `is_role` = 'y').|
| `Acl_table_grants` | 부여 된 테이블 권한 수 (`mysql.tables_priv` 테이블의 행). |
| `Acl_users` | 사용자 수 (`mysql.user` 테이블의 행은 `is_role = 'n'`). |
| `Busy_time` | 연결에 대한 활동 초의 누적 시간. |
| `Key_blocks_not_flushed` | 수정되었지만 디스크로 플러시되지 않은 키 캐시 블록 수. |
| `Key_blocks_unused` | 사용하지 않는 키 캐시 블록 수.|
| `Key_blocks_used` | 동시에 사용 된 주요 캐시 블록의 최대 수.|
| `Key_blocks_warm` | 따뜻한 목록의 키 캐시 블록 수.|
| `Key_read_requests` | 키 캐시 블록 수를 읽습니다. `key_buffer_size` 최적화를 참조하십시오.|
| `Key_reads` | 물리적 인덱스 블록의 수. `key_buffer_size` 최적화를 참조하십시오.|
| `Key_write_requests` | 키 캐시에 블록을 작성하라는 요청 수. |
| `Key_writes` | 키 캐시 블록 수의 수는 쓰기 요청입니다|
| `Last_query_cost` | 가장 최근의 쿼리 최적화 쿼리 비용 계산. 하위 쿼리 또는 노조와 같은 복잡한 쿼리에 대해 계산할 수 없습니다. 복잡한 쿼리의 경우 0으로 설정됩니다.|
| `Max_statement_time_exceeded` | `max_statement_time`에서 지정된 실행 시간을 초과 한 쿼리 수입니다. 실행하는 데 시간이 오래 걸리는 낙태 진술을 참조하십시오. |
| `Max_used_connections` | 최대 연결 수는 동시에 열립니다. 글로벌 값은 플러시 상태로 플러시 될 수 있습니다.|
| `Memory_used` | 바이트의 글로벌 또는 연결 메모리 사용. 여기에는 모든 연결 당 메모리 할당이 포함되지만 `key_buffer`, `innodb_buffer_pool` 등과 같은 글로벌 할당을 제외합니다.|
| `Memory_used_initial` | 서버가 사용자 연결을 서비스하기 시작했을 때 사용 된 메모리 양.|
| `Not_flushed_delayed_rows` | 삽입 된 지연 행의 수 쓰기를 기다리고 있습니다. |
| `Open_files` | 현재 서버에서 열린 일반 파일 수. 내부 기능을 사용하는 소켓이나 파이프 또는 스토리지 엔진은 포함되지 않습니다.|
| `Open_streams` | 현재 열린 스트림, 일반적으로 로그 파일. |
| `Open_table_definitions` | 현재 캐시 된 .frm 파일 수.|
| `Open_tables` | 임시 테이블을 제외한 현재 열린 테이블 수.|
| `Opened_files` | 서버가 열린 파일 수.|
| `Opened_plugin_libraries` | 서버가 플러그인을로드하기 위해 열린 공유 라이브러리 수. |
| `Opened_table_definitions` | 캐시 된 .frm 파일 수. |
| `Opened_tables` | 서버가 열린 테이블 수. |
| `Opened_views` | 서버가 열린 조회수 수. |
| `Prepared_stmt_count` | 현재 준비된 진술의 수. |
| `Qcache_free_blocks` | 무료 쿼리 캐시 메모리 블록 수.|
| `Qcache_free_memory` | 무료 쿼리 캐시 메모리의 양.|
| `Qcache_hits` | 쿼리 캐시를 히트한 수. (MySQL 8.0에서는 Query Cache 기능 사라짐) |
| `Qcache_inserts` | 쿼리 캐시에 캐시 된 쿼리 수.글로벌 값은 플러시 상태로 플러시 될 수 있습니다. |
| `Qcache_lowmem_prunes` | 쿼리 캐시에서 새로운 결과를위한 공간을 만들기 위해 오래된 결과를 제거하기 위해 수행 된 가지 치기 작업 수입니다. 글로벌 값은 플러시 상태로 플러시 될 수 있습니다.|
| `Qcache_not_cached` | 쿼리 캐시에서 성취 할 수없는 쿼리 수 또는 `sql_no_cache를` 사용하십시오. 글로벌 값은 플러시 상태로 플러시 될 수 있습니다.|
| `Qcache_queries_in_cache` | 현재 쿼리 캐시에 의해 캐시 된 쿼리 수입니다.|
| `Qcache_total_blocks` | 쿼리 캐시에서 사용하는 블록 수.|
| `Tc_log_max_pages_used` | 최대 메모리 매핑 파일 기반 트랜잭션 코디네이터 로그에서 사용하는 페이지 수글로벌 값은 플러시 상태로 플러시 될 수 있습니다.|
| `Tc_log_page_size` | 메모리 매핑 된 파일 기반 트랜잭션 코디네이터 로그의 페이지 크기. |
| `Tc_log_page_waits` | 2 단계 커밋이 무료 메모리 매핑 파일 기반 트랜잭션 코디네이터 로그 페이지를 기다려야했습니다. 글로벌 값은 플러시 상태로 플러시 될 수 있습니다. |

</div>