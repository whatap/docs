---
id: java-2_2_60
title: Java Agent v2.2.60
toc_max_heading_level: 2
pagination_next: release-notes/java/java-2_2_59
displayed_sidebar: releaseSidebar
---

2025년 05월 29일

- <Status priority='1'>New</Status> `spring-cloud-openfeign-core`, `spring-cloud-circuitbreaker-resilience4j` 추적

    ```String
    weaving=feign-11,resilience4j-circuitbreaker
    ```

- <Status priority='1'>New</Status> `feign-hystrix` 추적

    ```String
    weaving=feign-11,feign-hystrix,hystrix-circuitbreaker
    ```

- <Status priority='1'>New</Status> `log4j-1.x` 로그 수집

    ```String
    weaving=log4j-1
    ```

    * `log4j-1.x` 로그 수집 설정

        ```String
        # default: false
        # trace exception stack
        trace_log4j_stack_enabled=true

        # default: true
        # trace standard out log
        trace_log4j_stdout_enabled=true

        # default: true
        # trace log4j appender log
        trace_log4j_appender_enabled=true

        # example: ignore appender accessfile, file
        # ignore_log4j_appender_set=accessfile,file
        ignore_log4j_appender_set=
        ```

- <Status priority='1'>Changed</Status> Tomcat log로 수집하는 AppServer 카테고리의 level 데이터를 Tomcat log level에 맞게 수정

    * warn → WARNING

    * error, fatal → SEVERE

- <Status priority='0'>Fixed</Status> `db_pool_detail` 메트릭스의 class 데이터 오타 수정(HyrisDataSourcePool → HikariPool)

- <Status priority='0'>Fixed</Status> Feign Client의 응답이 없는 경우 및 에러로 수집하지 못하는 경우 수정

import DownloadJava from './_import_dl-java-agent.mdx';

<DownloadJava ver="2.2.60" />