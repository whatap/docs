---
id: server-inventory
title: 서버 구성 정보
description: 서버 구성 정보 메뉴를 안내합니다. 
keywords: [ 서버 상세, 서버 구성 정보, 분석하기, UI, Server ]
---

홈 화면 &gt; 프로젝트 선택 &gt; <Cmdname sid="side_serverList" className="uitext" /> &gt; <Cmdname sid="LNK08084" className="uitext" />

{/* :::note 

**지원 에이전트 버전**

<Cmdname sid="LNK08084" className="uitext" /> 메뉴는 특정 에이전트 버전 이상에서만 지원됩니다. 각 운영체제별 최소 지원 버전은 다음과 같습니다. 


* **Linux**: 2.6.6 이상

* **AIX**: 1.3.4 이상

* **Solaris**: 1.3.4 이상

* **Windows**: 2.6.6 이상

::: */}

와탭 모니터링 서비스 초기 화면에서 프로젝트를 선택한 다음 <Cmdname sid="side_serverList" className="uitext" /> 메뉴 하위에 <Cmdname sid="LNK08084" className="uitext" /> 메뉴를 선택하세요. <Cmdname sid="LNK08084" className="uitext" />는 서버 운영에 필요한 구성 정보를 제공하고 관리 편의성을 위해 수동으로 관리 항목을 추가할 수 있습니다. 

<Cmdname sid="LNK08084" className="uitext" />는 서버의 물리적 및 가상적 특성, 운영 환경 등을 체계적으로 관리하기 위한 메뉴입니다. 이 기능은 자동으로 수집된 서버 구성 요소와 사용자 정의 항목을 통해 서버의 구성을 상세히 파악하고 관리할 수 있게 해줍니다. <Cmdname sid="LNK08084" className="uitext" />는 서버의 성능 분석과 문제 해결에 필수적인 데이터를 제공합니다.

## 서버 구성 정보 조회 활성화

### 지원 에이전트 및 업데이트

<Cmdname sid="LNK08084" className="uitext" /> 메뉴는 운영체제별로 다음 버전 이상에서만 지원됩니다. 

* **Linux**: 2.6.6 이상

  <Tabs>
  <TabItem value="du" label="Debian/Ubuntu" default>

  ```bash
  sudo apt-get update
  sudo apt-get install whatap-infra
  ```

  </TabItem>
  <TabItem value="arfocrs" label="Amazon Linux/RHEL/Fedora/Oracle Linux/CentOS/Rocky Linux/SUSE">

  ```bash
  sudo yum update whatap-infra
  ```

  </TabItem>
  </Tabs> 

* **Linux**: 1.3.4 이상

  <Tabs>
  <TabItem value="aix" label="AIX" default>

  ```bash
  stopsrc -s whatap-infra
  cp /usr/whatap/infra/conf/whatap.conf /tmp/whatap.conf
  rpm -e whatap-infra
  rpm -Uvh http://repo.whatap.io/aix/noarch/whatap-infra-latest.noarch.rpm
  cp /tmp/whatap.conf /usr/whatap/infra/conf/whatap.conf
  ```

  </TabItem>
  <TabItem value="solaris" label="Oracle Solaris">

  ```bash
  cp /usr/whatap/infra/conf/whatap.conf /tmp
  /etc/init.d/whatap-infra stop
  pkgrm whatap-infra
  wget https://repo.whatap.io/sunos/11/whatap-infra.latest.SPARC.pkg.tar.gz
  tar xzvf whatap-infra.latest.SPARC.pkg.tar.gz
  pkgadd -d .
  cp /tmp/whatap.conf /usr/whatap/infra/conf/whatap.conf
  ```

  </TabItem>
  </Tabs> 

* **Windows**: 2.6.6 이상

  설치 파일을 다음 [링크](https://repo.whatap.io/windows/whatap_infra.exe)를 통해 다운로드 후 실행하세요. 에이전트 업데이트 후 서비스가 재시작됩니다. 

### 서버 구성 정보 옵션 설정 

서버 구성 정보를 조회하려면 다음 설정이 필요합니다.

1. *whatap.conf* 파일 내 다음 옵션을 활성화하세요. 

    ```ini 
    hostinfov2.enabled=true
    ```

1. 환경별로 옵션 추가 설정 후 에이전트를 재시작하세요. 

    * **Linux**

      <Tabs>
      <TabItem value="linux-opt" label="옵션 추가 명령어" default>

      ```bash
      grep -q '^hostinfov2.enabled' /usr/whatap/infra/conf/whatap.conf || echo 'hostinfov2.enabled=true' | sudo tee -a /usr/whatap/infra/conf/whatap.conf
      ```

      </TabItem>
      <TabItem value="linux-restart" label="에이전트 재시작">

      ```bash
      sudo service whatap-infra restart
      ```

      </TabItem>
      </Tabs> 

    * **Unix** 

      <Tabs>
      <TabItem value="unix-opt" label="옵션 추가 명령어" default>

      ```bash title='AIX/Oracle Solaris'
      grep -q '^hostinfov2.enabled' /usr/whatap/infra/conf/whatap.conf || echo 'hostinfov2.enabled=true' | tee -a /usr/whatap/infra/conf/whatap.conf
      ```

      </TabItem>
      <TabItem value="unix-restart" label="에이전트 재시작">

      ```bash title='AIX'
      stopsrc -s whatap-infra
      startsrc -s whatap-infra
      ```

      ```bash title='Oracle Solaris'
      /etc/init.d/whatap-infra restart
      ```

      </TabItem>
      </Tabs> 

    * **Windows**

      <Tabs>
      <TabItem value="windows-opt" label="옵션 추가 명령어" default>

      ```bash
      if (-not (Select-String -Path "C:Program FilesWhatapInfrawhatap.conf" -Pattern "^hostinfov2.enabled" -Quiet)) { Add-Content -Path "C:Program FilesWhatapInfrawhatap.conf" -Value "hostinfov2.enabled=true" }
      ```

      </TabItem>
      <TabItem value="windows-restart" label="에이전트 재시작">

      ```bash
      Restart-Service -Name "Whatap Infra"
      ```

      </TabItem>
      </Tabs> 

## 서버 구성 정보

대시보드 및 주요 제공 정보 안내 

### 필터 

상단 필터 안내

### 컬럼 설정 

사용자 정의 컬럼 설정 안내 필요 
